export class Serie {
    constructor(numero, nombre, distribuidora, temporadas, resumen, link, imagen) {
        this.nombre = nombre;
        this.numero = numero;
        this.distribuidora = distribuidora;
        this.link = link;
        this.resumen = resumen;
        this.temporadas = temporadas;
        this.imagen = imagen;
    }
}
